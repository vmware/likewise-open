#!/bin/bash

BUILD_ROOT=`(cd $(dirname $0)/../../.. && pwd)`
export BUILD_ROOT

umask 022

PKG="libs"
COMPONENTS="krb5 openldap lwbase libunistr \
            lwmsg centutils lwmapsecurity \
            libkeytab libgss"

for comp in ${COMPONENTS}; do
    echo "Populating files from $comp..."
    (${BUILD_ROOT}/build/populate ${PKG} $comp)
done
